1. Basics of array - array definition - basic terminologies - maximum size of an array 

2. Largest element in the array 

  -BRUTE SOLUTION
    -1. sort
    -2. print
    -3. T.C = O(nlogn)

  -OPTIMAL SOLUTION
     -1. assign a[0] as lasrgest
     -2. check till n-1 if it is the lasrgest
     -3. print
     -4. T.C = O(n)

Source code :

#include <bits/stdc++.h>
using namespace std;

int Largestelement(vector<int> a, int n )
{
    int largest = a[0];
    
    for(int i = 0; i < n ; i++)
    {
        if(largest < a[i])
        {
            largest = a[i];
        }
    }

    return largest;
}

int main()
{
    int n;
    cin >> n;

    vector<int> a(n);
    for(int i = 0; i < n; i++)
    {
        cin >> a[i];
    }

    cout << Largestelement(a,n);

    return 0;
}


3. Second Largest Element in the array 

   -BRUTE SOLUTION
      -1. Sort the array
      -2. check for(i = n-2 ; i >=0 ; i--) if arr[i] is not equal to largest then arr[i] = second largest
      -3. T.C = sorting + checking if second largest exist = O(nlogn + n)


    -BETTER SOLUTION
       -1. find largest in the first pass
       -2. assume slargest to be -1
       -3. check if the element is larger than slargest and smaller than largest
       -4. T.C = O(2n)

source code 

#include <bits/stdc++.h>
using namespace std;

int Slargestelement(vector<int> a, int n )
{
    int slargest = -1;
    int largest = a[0];
    
    for(int i = 0; i < n ; i++)
    {
        if(largest < a[i])
        {
            largest = a[i];
        }
    }

    for(int i = 0 ; i< n ; i++)
    {
        if(a[i]>slargest && a[i] != largest)
        {
            slargest = a[i];
        }
    }

    return slargest;
}

int main()
{
    int n;
    cin >> n;

    vector<int> a(n);
    for(int i = 0; i < n; i++)
    {
        cin >> a[i];
    }

    cout << Slargestelement(a,n);

    return 0;
}
 

    -OPTIMAL SOLUTION
        -1. T.C = O(N)


sorce code :

#include <bits/stdc++.h>
using namespace std;

int Slargestelement(vector<int> a, int n )
{
    int slargest = -1;
    int largest = a[0];
    
    for(int i = 1; i < n ; i++)
    {
        if(a[i] > largest)
        {
            slargest = largest;
            largest = a[i];
        }
        else if(a[i] < largest && a[i] > slargest)
        {
            slargest = a[i];
        }
    }

    return slargest;
}

int main()
{
    int n;
    cin >> n;

    vector<int> a(n);
    for(int i = 0; i < n; i++)
    {
        cin >> a[i];
    }

    cout << Slargestelement(a,n);

    return 0;
}
 

4. Check if array is sorted 


  -1. check if the previous is smaller than or equal to next 
  -2. T.C = o(n)

source code :

#include <bits/stdc++.h>
using namespace std;

bool isSorted(vector<int> a , int n)
{
    for(int i = 0; i < n ; i++)
    {
        if(a[i]<a[i+1] || a[i]== a[i+1])
        {
            
        }
        else return false;
    }

    return true;
}

int main()
{
    int n;
    cin >> n;

    vector<int> a(n);
    for(int i = 0; i < n; i++)
    {
        cin >> a[i];
    }

    cout << isSorted(a,n);

    return 0;
}
 


5. Remove Duplicates from an array 


   -BRUTE FORCE

#include <bits/stdc++.h>
using namespace std;

vector<int> removeduplicate(vector<int> a , int n)
{
    set<int> st;

    for(int i = 0; i < n ; i++)
    {
        st.insert(a[i]);
    }

    int index = 0;
    for(auto it: st)
    {
        a[index] = it;
        index++;
    }

    a.resize(index);   // keep only unique elements
    return a;
}

int main()
{
    int n;
    cin >> n;

    vector<int> a(n);

    for(int i = 0; i < n; i++)
    {
        cin >> a[i];
    }

    vector<int> result = removeduplicate(a, n);

    for(int x : result)
    {
        cout << x << " ";
    }

    return 0;
}


   -BETTER SOL  

   -OPTIMAL SOL
